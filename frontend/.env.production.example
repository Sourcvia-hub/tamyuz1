# Production Frontend Environment Variables
# Copy these values to your deployed frontend environment

# Backend API URL (REQUIRED - Update with your actual production backend URL)
REACT_APP_BACKEND_URL=https://sourcevia-mgmt.emergent.host

# Emergent Platform Configuration (Keep these as-is)
WDS_SOCKET_PORT=443
REACT_APP_ENABLE_VISUAL_EDITS=false
ENABLE_HEALTH_CHECK=false
REACT_APP_DEV_MODE=false

# IMPORTANT NOTES:
# 1. REACT_APP_BACKEND_URL should point to your production backend
# 2. Do NOT include /api in the URL - it will be added automatically
# 3. No trailing slash in the URL
# 4. Must be set before building/deploying the frontend
