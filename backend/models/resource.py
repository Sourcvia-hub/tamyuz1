"""\nResource models\n"""\nfrom pydantic import BaseModel, Field, ConfigDict, EmailStr\nfrom typing import Optional, List\nfrom datetime import datetime, timezone\nfrom enum import Enum\nimport uuid\n\n\nclass WorkType(str, Enum):\n    FULL_TIME = "full_time"\n    PART_TIME = "part_time"\n    CONTRACT = "contract"\n\n\nclass ResourceStatus(str, Enum):\n    ACTIVE = "active"\n    INACTIVE = "inactive"\n    ON_LEAVE = "on_leave"\n\n\nclass Relative(BaseModel):\n    name: str\n    relationship: str\n    nationality: str\n\n\nclass Resource(BaseModel):\n    model_config = ConfigDict(extra="ignore")\n    \n    id: str = Field(default_factory=lambda: str(uuid.uuid4()))\n    resource_number: Optional[str] = None  # Auto-generated (e.g., RES-25-0001)\n    \n    # Basic Information\n    name: str\n    email: EmailStr\n    mobile: str\n    national_id: str\n    iqama_number: Optional[str] = None\n    nationality: str\n    \n    # Family Information\n    relatives: List[Relative] = []\n    \n    # Employment Details\n    position: str\n    department: str\n    work_type: WorkType\n    contract_id: Optional[str] = None  # Link to contract if outsourced\n    vendor_id: Optional[str] = None  # If outsourced staff\n    \n    # Contract Dates\n    contract_start_date: datetime\n    contract_end_date: datetime\n    \n    # Status\n    status: ResourceStatus = ResourceStatus.ACTIVE\n    \n    # Documents\n    documents: List[str] = []\n    \n    # System fields\n    created_by: Optional[str] = None\n    created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))\n    updated_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))\n