# Production Backend Environment Variables
# For deployment: EMT-496c37 (sourcevia-secure)
# Backend URL: https://sourcevia-mgmt.emergent.host

# CORS Origins (CRITICAL for frontend to work)
CORS_ORIGINS=https://sourcevia.xyz,https://www.sourcevia.xyz,https://sourcevia-mgmt.emergent.host

# MongoDB Atlas Connection (REQUIRED - Replace with your actual credentials)
MONGO_URL=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/sourcevia?retryWrites=true&w=majority

# Emergent LLM Key
EMERGENT_LLM_KEY=sk-emergent-e9d7eEd061b2fCeDbB

# IMPORTANT NOTES:
# 1. Replace YOUR_USERNAME, YOUR_PASSWORD, YOUR_CLUSTER in MONGO_URL
# 2. Database name MUST be 'sourcevia' in the URL path
# 3. CORS_ORIGINS includes all domains that need to access the API
# 4. No spaces in CORS_ORIGINS (comma-separated only)
