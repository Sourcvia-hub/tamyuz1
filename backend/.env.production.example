# Production Backend Environment Variables
# Copy these values to your deployed backend environment

# MongoDB Atlas Connection (REQUIRED - Replace with your actual MongoDB Atlas URL)
MONGO_URL=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/sourcevia?retryWrites=true&w=majority

# CORS Origins (REQUIRED - Update with your actual production domains)
CORS_ORIGINS=https://sourcevia.xyz,https://www.sourcevia.xyz

# Emergent LLM Key (Already configured - don't change)
EMERGENT_LLM_KEY=sk-emergent-e9d7eEd061b2fCeDbB

# IMPORTANT NOTES:
# 1. Replace YOUR_USERNAME, YOUR_PASSWORD, YOUR_CLUSTER with your actual MongoDB Atlas credentials
# 2. Keep the database name "sourcevia" in the URL path (after .net/)
# 3. Do NOT set MONGO_DB_NAME - it will be extracted from MONGO_URL automatically
# 4. CORS_ORIGINS should include all domains that will access your backend (comma-separated, no spaces)
