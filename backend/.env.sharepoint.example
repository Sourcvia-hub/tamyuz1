# ProcureFlix SharePoint Configuration Example
# Copy this file to backend/.env and fill in your actual values

# ==============================================================================
# Data Backend Selection
# ==============================================================================
# Options: "memory" (default) or "sharepoint"
# - memory: Uses in-memory JSON-seeded data (for demos/dev)
# - sharepoint: Uses SharePoint Online lists (for production)
PROCUREFLIX_DATA_BACKEND=sharepoint

# ==============================================================================
# SharePoint Authentication (Client Credentials Flow)
# ==============================================================================
# Required when PROCUREFLIX_DATA_BACKEND=sharepoint

# SharePoint site URL (include /sites/SiteName)
# Example: https://contoso.sharepoint.com/sites/ProcureFlix
SHAREPOINT_SITE_URL=https://<your-tenant>.sharepoint.com/sites/<YourSiteName>

# Azure AD tenant ID (from Azure Portal → Azure AD → Overview)
# Example: 12345678-1234-1234-1234-123456789012
SHAREPOINT_TENANT_ID=__TO_BE_CONFIGURED__

# App registration client ID (from Azure Portal → App registrations)
# Example: 87654321-4321-4321-4321-210987654321
SHAREPOINT_CLIENT_ID=__TO_BE_CONFIGURED__

# App registration client secret (from Azure Portal → Certificates & secrets)
# Important: Never commit this value to source control!
SHAREPOINT_CLIENT_SECRET=__TO_BE_CONFIGURED__

# ==============================================================================
# Additional Settings
# ==============================================================================

# Enable AI features (for future use)
PROCUREFLIX_ENABLE_AI=true

# ==============================================================================
# Setup Instructions
# ==============================================================================
#
# 1. Create an Azure AD App Registration:
#    - Go to Azure Portal → Azure AD → App registrations → New registration
#    - Note the Application (client) ID and Directory (tenant) ID
#    - Create a client secret under Certificates & secrets
#
# 2. Grant SharePoint Permissions:
#    - In the app registration, go to API permissions
#    - Add SharePoint Application permission: Sites.ReadWrite.All
#    - Grant admin consent
#
# 3. Create SharePoint Lists:
#    - Create lists: Vendors, Tenders, TenderProposals, Contracts,
#      PurchaseOrders, Invoices
#    - Add required columns as specified in SHAREPOINT_INTEGRATION.md
#
# 4. Test Connection:
#    - Start the backend: python server.py
#    - Check health: curl http://localhost:8001/api/procureflix/health
#    - Should return: {"data_backend": "sharepoint", "status": "ok"}
#
# For detailed setup instructions, see: /app/SHAREPOINT_INTEGRATION.md
